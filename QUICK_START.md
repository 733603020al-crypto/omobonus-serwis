# 🚀 Быстрый запуск после обновления безопасности

## После обновления React до 19.2.3

### 1. Очистка кеша (обязательно!)
```bash
npm run clean
```

Или вручную:
```powershell
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .turbo -ErrorAction SilentlyContinue
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Запуск dev-сервера
```bash
npm run dev:safe
```

Или обычный запуск:
```bash
npm run dev
```

### 4. Открыть в браузере
После запуска откройте: http://localhost:3000

---

## Если возникает ошибка ERR_CONNECTION_REFUSED

1. **Проверьте, что порт 3000 свободен:**
   ```powershell
   Get-NetTCPConnection -LocalPort 3000
   ```

2. **Остановите все процессы Node.js:**
   ```powershell
   Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force
   ```

3. **Очистите кеш и перезапустите:**
   ```bash
   npm run clean
   npm run dev:safe
   ```

---

## Проверка работы

После запуска проверьте:
- ✅ Главная страница: http://localhost:3000
- ✅ Страница услуги: http://localhost:3000/uslugi/serwis-komputerow-stacjonarnych
- ✅ Форма контакта работает
- ✅ Нет ошибок в консоли браузера

---

## Статус безопасности

✅ **Все уязвимости устранены:**
- CVE-2025-55184 (DoS) - исправлено
- CVE-2025-55183 (Source Code Exposure) - исправлено

**Версии:**
- Next.js: 15.5.9
- React: 19.2.3
- React DOM: 19.2.3



