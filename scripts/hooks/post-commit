#!/bin/sh

# Git post-commit hook - БЕЗОПАСНАЯ ВЕРСИЯ
# Не запускает процессы в фоне, чтобы не блокировать Cursor
# Просто выводит информацию и завершается немедленно

echo ""
echo "✅ Git commit выполнен успешно"
echo "💡 Для запуска dev-сервера используйте: npm run dev"
echo ""

exit 0
