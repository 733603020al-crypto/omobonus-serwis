# Рекомендации по улучшению производительности сайта

## 📊 Текущие показатели PageSpeed Insights (Mobile)

- **Performance:** 77 (оранжевый) - нужно улучшить до 90+
- **Accessibility:** 95 (зеленый) - хорошо
- **Best Practices:** 100 (зеленый) - отлично
- **SEO:** 100 (зеленый) - отлично

## ✅ Уже исправлено

1. **Accessibility:**
   - ✅ Добавлены `aria-label` для кнопок без текста
   - ✅ Добавлены `aria-expanded` и `aria-haspopup` для выпадающих списков

2. **Оптимизация изображений:**
   - ✅ Убран `unoptimized` с изображений в секции услуг
   - ✅ Добавлен `loading="lazy"` для некритических изображений

3. **Загрузка шрифтов:**
   - ✅ Добавлен `preconnect` для Google Fonts

## 🔴 Критичные проблемы (требуют внимания)

### 1. LCP (Largest Contentful Paint) - 4.9s ❌
**Цель:** < 2.5s

**Проблема:** Самый большой элемент контента загружается слишком медленно.

**Решения:**
- ✅ Использовать `priority` для критических изображений (уже используется для hero)
- ⚠️ Проверить, какие изображения являются LCP элементом
- ⚠️ Оптимизировать размер критических изображений
- ⚠️ Использовать WebP/AVIF форматы (уже настроено в next.config.js)
- ⚠️ Добавить `fetchpriority="high"` для критических ресурсов

### 2. Большие изображения - 31,051 KiB можно уменьшить ⚠️

**Решения:**
1. **Оптимизировать существующие PNG изображения:**
   ```bash
   # Использовать инструменты для сжатия:
   # - ImageOptim (Mac)
   # - TinyPNG (онлайн)
   # - Squoosh (онлайн)
   ```

2. **Конвертировать в WebP/AVIF:**
   - Next.js автоматически конвертирует, но исходные файлы должны быть меньше
   - Проверить большие изображения в `/public/images/`

3. **Использовать responsive images:**
   - Убедиться, что все изображения используют правильные `sizes`
   - Использовать `srcset` для разных размеров экранов

### 3. Render-blocking requests - 750ms ускорение ⚠️

**Решения:**
1. **Оптимизировать загрузку шрифтов:**
   ```typescript
   // В layout.tsx уже добавлен preconnect
   // Можно добавить font-display: swap в CSS
   ```

2. **Добавить preload для критических ресурсов:**
   ```tsx
   <link rel="preload" href="/images/Logo_Omobonus.png" as="image" />
   <link rel="preload" href="/images/Background_1.png" as="image" />
   ```

3. **Отложить загрузку некритических CSS:**
   - Проверить, нет ли блокирующих CSS файлов
   - Использовать `media="print"` для некритических стилей

### 4. Общий размер сети - 32,495 KiB ⚠️

**Решения:**
1. **Оптимизировать все изображения** (см. пункт 2)
2. **Использовать code splitting:**
   - Убедиться, что большие компоненты загружаются лениво
   - Проверить размер JavaScript bundle

3. **Включить сжатие на сервере:**
   - Gzip/Brotli compression
   - Проверить настройки Vercel

### 5. Длинные задачи в главном потоке - 2 задачи ⚠️

**Решения:**
1. **Использовать Web Workers для тяжелых вычислений**
2. **Оптимизировать JavaScript:**
   - Проверить, нет ли синхронных операций
   - Использовать `requestIdleCallback` для некритических задач

3. **Оптимизировать React рендеринг:**
   - Использовать `React.memo` для компонентов
   - Проверить ненужные ре-рендеры

## 📝 Дополнительные рекомендации

### Оптимизация изображений

1. **Проверить размеры файлов:**
   ```bash
   # Найти большие изображения
   find public/images -type f -size +500k
   ```

2. **Оптимизировать вручную:**
   - Использовать Squoosh.app для оптимизации
   - Конвертировать PNG в WebP
   - Уменьшить качество для некритических изображений

### Оптимизация шрифтов

1. **Использовать font-display: swap:**
   ```css
   @font-face {
     font-display: swap;
   }
   ```

2. **Подгружать только необходимые веса:**
   - Проверить, все ли веса шрифтов используются
   - Удалить неиспользуемые веса

### Оптимизация JavaScript

1. **Проверить размер bundle:**
   ```bash
   npm run build
   # Посмотреть размеры в .next/analyze
   ```

2. **Использовать динамические импорты:**
   ```tsx
   const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
     loading: () => <p>Loading...</p>
   })
   ```

## 🎯 Приоритеты

1. **Высокий приоритет:**
   - ⚠️ Улучшить LCP (оптимизировать критическое изображение)
   - ⚠️ Уменьшить размер изображений (31,051 KiB)
   - ⚠️ Убрать render-blocking ресурсы

2. **Средний приоритет:**
   - ⚠️ Уменьшить общий размер сети
   - ⚠️ Оптимизировать длинные задачи

3. **Низкий приоритет:**
   - ✅ Accessibility уже хорошо (95)
   - ✅ SEO отлично (100)

## 📈 Ожидаемые результаты

После выполнения всех рекомендаций:
- **Performance:** 77 → 85-90+
- **LCP:** 4.9s → < 2.5s
- **Размер сети:** 32,495 KiB → ~20,000 KiB
- **Accessibility:** 95 → 100 (после добавления всех aria-label)

## 🔧 Инструменты для проверки

1. **PageSpeed Insights:** https://pagespeed.web.dev/
2. **Lighthouse (Chrome DevTools):** F12 → Lighthouse
3. **WebPageTest:** https://www.webpagetest.org/
4. **Bundle Analyzer:** `npm install @next/bundle-analyzer`

