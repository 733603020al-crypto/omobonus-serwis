# Анализ логики аккордеона для секции "Naprawy i usługi serwisowe (opcjonalne)"

## 📋 Структура аккордеона

### 1️⃣ Уровни аккордеона

**Двухуровневая структура:**
1. **Секции (Sections)** - верхний уровень (например, "Naprawy i usługi serwisowe")
2. **Подкатегории (Subcategories)** - вложенный уровень (например, "Mechanizm uderzeniowy...")

### 2️⃣ Управление состоянием

```typescript
// Состояние открытых элементов
const [openSection, setOpenSection] = useState<string | null>(null)      // ID открытой секции
const [openSubcategory, setOpenSubcategory] = useState<string | null>(null)  // ID открытой подкатегории

// Refs для прокрутки
const sectionRefs = useRef<ScrollRefs>({})        // Refs для секций
const subcategoryRefs = useRef<ScrollRefs>({})    // Refs для подкатегорий
```

**Константы:**
- `SECTION_SCROLL_OFFSET = 120` - отступ от верха при прокрутке

---

## 🔍 Как работает открытие/закрытие

### Открытие секции (верхний уровень)

```typescript
// Функция обработки клика на секцию
const handleSectionChange = (value: string | null) => {
  setOpenSection(prev => (prev === value ? null : value))
  setOpenSubcategory(null)  // ⚠️ При открытии новой секции закрываются подкатегории
  // ...
}
```

**Эффект для прокрутки секции:**
```typescript
useEffect(() => {
  if (!openSection) return
  scrollIntoViewIfNeeded(sectionRefs.current[openSection], SECTION_SCROLL_OFFSET)
}, [openSection])
```

✅ **Работает**: При открытии секции она прокручивается к верху с отступом 120px.

---

### Открытие подкатегории (вложенный уровень)

**Для секции "naprawy" используется отдельный Accordion:**
```typescript
// Функция обработки клика на подкатегорию
const handleSubcategoryChange = (sectionId: string, value: string | null) => {
  if (sectionId !== 'naprawy') return
  setOpenSubcategory(prev => (prev === value ? null : value))
}

// Получение значения для Accordion
const getSubcategoryValue = (sectionId: string) =>
  sectionId === 'naprawy' ? openSubcategory ?? undefined : undefined
```

**Рендеринг подкатегорий:**
```typescript
{isRepairSection && (
  <Accordion
    type="single"
    collapsible
    value={getSubcategoryValue(section.id)}
    onValueChange={value => handleSubcategoryChange(section.id, value)}
    className="w-full"
  >
    {subcategoryItems}
  </Accordion>
)}
```

**Эффект для прокрутки подкатегории:**
```typescript
useEffect(() => {
  if (!openSubcategory || openSection !== 'naprawy') return
  const parentRef = sectionRefs.current['naprawy']
  if (!parentRef) return

  const rect = parentRef.getBoundingClientRect()
  if (rect.bottom > 70) {  // ⚠️ ПРОБЛЕМА: проверка только нижней границы
    return
  }

  scrollIntoViewIfNeeded(parentRef, SECTION_SCROLL_OFFSET, true)
}, [openSubcategory, openSection])
```

---

## ❌ Проблема: почему подблок не поднимается под заголовок

### Анализ текущей логики

1. **Эффект для подкатегорий прокручивает родительскую секцию, а не саму подкатегорию:**
   ```typescript
   // ❌ Прокручивается секция "naprawy", а не открытая подкатегория
   scrollIntoViewIfNeeded(parentRef, SECTION_SCROLL_OFFSET, true)
   ```

2. **Условие проверки работает неправильно:**
   ```typescript
   if (rect.bottom > 70) {  // Проверяет только нижнюю границу секции
     return  // Если нижняя граница ниже 70px, НЕ прокручивает
   }
   ```
   **Проблема:** Эта проверка срабатывает только если секция уже прокручена почти вверх. Но если секция открыта, но не прокручена - она не сработает.

3. **Ref для подкатегорий не используется для прокрутки:**
   ```typescript
   // Refs привязываются к AccordionItem подкатегорий
   ref={node => {
     subcategoryRefs.current[subcategory.id] = node
   }}
   ```
   Но эти refs **никогда не используются** для прокрутки! ❌

4. **Структура DOM:**
   ```
   AccordionItem (section) [ref: sectionRefs.current['naprawy']]
     └─ AccordionContent [max-h-[70vh] overflow-y-auto]  ⚠️ Контейнер с overflow
        └─ Accordion (для подкатегорий)
           └─ AccordionItem (subcategory) [ref: subcategoryRefs.current[...]]
              └─ AccordionTrigger
              └─ AccordionContent
   ```
   
   **Проблема:** Подкатегория находится внутри `AccordionContent` с `overflow-y-auto` и `max-h-[70vh]`. Это создает отдельный скролл-контейнер, который не связан с прокруткой страницы!

---

## 🔧 Что мешает "прилипанию"

### 1. **Контейнер с overflow-y-auto**

```typescript
<AccordionContent 
  className={cn(
    "pb-3 max-h-[70vh] overflow-y-auto scroll-smooth ..."  // ⚠️
  )}
>
```

**Проблема:** 
- Когда подкатегория открывается, она раскрывается внутри этого скролл-контейнера
- Прокрутка происходит внутри контейнера, а не на уровне страницы
- `scrollIntoViewIfNeeded` прокручивает `sectionRefs.current['naprawy']`, но содержимое уже находится в другом скролл-контексте

### 2. **Неправильная цель прокрутки**

Текущий код пытается прокрутить секцию, но:
- Если секция уже открыта и видна, прокрутка не происходит
- Подкатегория может быть вне видимой области скролл-контейнера
- Нет прокрутки к конкретной подкатегории

### 3. **Логика проверки `rect.bottom > 70`**

```typescript
const rect = parentRef.getBoundingClientRect()
if (rect.bottom > 70) {  // Если нижняя граница секции > 70px от верха
  return  // Не прокручиваем
}
```

**Проблема:**
- Эта проверка работает только когда секция уже прокручена почти вверх
- Если секция открыта, но находится ниже - проверка блокирует прокрутку
- Не учитывает позицию открытой подкатегории

---

## 📊 Функция scrollIntoViewIfNeeded

```typescript
const scrollIntoViewIfNeeded = (
  target?: HTMLDivElement | null,
  offset = SECTION_SCROLL_OFFSET,
  force = false,
) => {
  if (!target) return

  const measureAndScroll = () => {
    const rect = target.getBoundingClientRect()
    const topVisible = rect.top >= offset
    const bottomVisible = rect.bottom <= window.innerHeight - 20

    if (!force && topVisible && bottomVisible) {
      return  // ⚠️ Если элемент видим, не прокручиваем (кроме force=true)
    }

    const top = rect.top + window.scrollY - offset
    window.scrollTo({ top, behavior: 'smooth' })  // ⚠️ Прокрутка страницы, не контейнера
  }

  requestAnimationFrame(() => {
    requestAnimationFrame(measureAndScroll)  // Двойной RAF для правильного измерения
  })
}
```

**Проблема:**
- Функция прокручивает `window` (страницу), но подкатегории находятся в контейнере с `overflow-y-auto`
- Не работает с вложенными скролл-контейнерами

---

## ✅ Решения

### Вариант 1: Прокрутка к подкатегории вместо секции

Изменить эффект для подкатегорий:
```typescript
useEffect(() => {
  if (!openSubcategory || openSection !== 'naprawy') return
  
  // ✅ Прокручиваем к самой подкатегории, а не к секции
  const subcategoryRef = subcategoryRefs.current[openSubcategory]
  if (!subcategoryRef) return
  
  scrollIntoViewIfNeeded(subcategoryRef, SECTION_SCROLL_OFFSET, true)
}, [openSubcategory, openSection])
```

### Вариант 2: Прокрутка контейнера с overflow

Создать функцию для прокрутки внутри `AccordionContent`:
```typescript
const scrollSubcategoryIntoView = (subcategoryId: string) => {
  const subcategoryRef = subcategoryRefs.current[subcategoryId]
  const sectionRef = sectionRefs.current['naprawy']
  if (!subcategoryRef || !sectionRef) return
  
  // Найти AccordionContent (родитель с overflow-y-auto)
  const accordionContent = sectionRef.querySelector('[data-slot="accordion-content"]')
  if (!accordionContent) return
  
  const rect = subcategoryRef.getBoundingClientRect()
  const containerRect = accordionContent.getBoundingClientRect()
  
  // Прокрутить контейнер так, чтобы подкатегория была видна
  const scrollTop = accordionContent.scrollTop + rect.top - containerRect.top - 120
  accordionContent.scrollTo({ top: scrollTop, behavior: 'smooth' })
}
```

### Вариант 3: Убрать overflow-y-auto и использовать прокрутку страницы

Убрать ограничение высоты и overflow, чтобы прокрутка работала на уровне страницы:
```typescript
// Вместо max-h-[70vh] overflow-y-auto использовать обычную прокрутку страницы
<AccordionContent className="pb-3 ...">  // Без overflow-y-auto
```

---

## 📝 Резюме

### Текущее состояние:
1. ✅ Секции прокручиваются правильно
2. ❌ Подкатегории НЕ прокручиваются к заголовку секции
3. ❌ Refs для подкатегорий создаются, но не используются
4. ❌ Эффект прокручивает секцию, а не подкатегорию
5. ⚠️ Контейнер с `overflow-y-auto` создает отдельный скролл-контекст

### Основные проблемы:
1. **Неправильная цель прокрутки** - прокручивается секция вместо подкатегории
2. **Конфликт скролл-контекстов** - `overflow-y-auto` создает вложенный скролл
3. **Неиспользуемые refs** - `subcategoryRefs` создаются, но не используются для прокрутки
4. **Логика проверки** - `rect.bottom > 70` блокирует прокрутку в большинстве случаев

### Рекомендации:
- Использовать `subcategoryRefs` для прокрутки к конкретной подкатегории
- Либо прокручивать контейнер с `overflow-y-auto`, либо убрать его и использовать прокрутку страницы
- Изменить логику эффекта, чтобы он срабатывал всегда при открытии подкатегории



