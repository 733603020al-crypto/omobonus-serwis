# Проверка поведения при закрытии подкатегории аккордеона

**Дата проверки:** текущая версия кода после упрощения логики

**Цель:** Убедиться, что при закрытии подкатегории в секции "Naprawy i usługi serwisowe (opcjonalne)" не происходит никаких программных действий со скроллом.

---

## 1️⃣ Проверка функции handleSubcategoryChange

**Местоположение:** `src/app/uslugi/service-accordion.tsx`, строки 1587-1592

**Код:**
```typescript
const handleSubcategoryChange = (sectionId: string, value: string | null) => {
  if (sectionId !== 'naprawy') return
  
  // Просто изменяем состояние - при закрытии браузер естественно обработает изменение высоты
  setOpenSubcategory(prev => (prev === value ? null : value))
}
```

**Анализ при закрытии (value = null):**
- ✅ Функция НЕ вызывает `scrollIntoView`
- ✅ Функция НЕ вызывает `scrollSubcategoryToTop`
- ✅ Функция НЕ вызывает `scrollTo` или `scrollBy`
- ✅ Функция НЕ вызывает `window.scrollTo` или `container.scrollTop`
- ✅ Функция НЕ использует `setTimeout`, `setInterval`, `requestAnimationFrame`
- ✅ Функция только изменяет состояние React через `setOpenSubcategory`

**Вывод:** ✅ При закрытии `handleSubcategoryChange` пассивна — только меняет состояние.

---

## 2️⃣ Проверка useEffect для openSubcategory

**Местоположение:** `src/app/uslugi/service-accordion.tsx`, строки 1629-1644

**Код:**
```typescript
useEffect(() => {
  // Прокрутка только при открытии подкатегории
  if (!openSubcategory || openSection !== 'naprawy') {
    return
  }
  
  const sectionRef = sectionRefs.current['naprawy']
  const subcategoryRef = subcategoryRefs.current[openSubcategory]
  
  if (!sectionRef || !subcategoryRef) {
    return
  }

  // Прокручиваем подкатегорию к верху внутри контейнера
  scrollSubcategoryToTop(sectionRef, subcategoryRef, SECTION_SCROLL_OFFSET)
}, [openSubcategory, openSection])
```

**Анализ при закрытии (openSubcategory становится null):**
- ✅ Первая проверка: `if (!openSubcategory || openSection !== 'naprawy')` — **выполняется сразу**
- ✅ Функция делает `return` — **не доходит до вызова `scrollSubcategoryToTop`**
- ✅ `scrollSubcategoryToTop` **НЕ вызывается** при закрытии
- ✅ Нет `setTimeout`, `setInterval`, `requestAnimationFrame` в этом useEffect

**Вывод:** ✅ При закрытии useEffect пассивен — сразу возвращается, никаких действий со скроллом.

---

## 3️⃣ Проверка функции scrollSubcategoryToTop

**Местоположение:** `src/app/uslugi/service-accordion.tsx`, строки 1234-1310

**Когда вызывается:**
- Только из `useEffect` для `openSubcategory` (строка 1643)
- Вызывается только когда `openSubcategory !== null` и `openSection === 'naprawy'`

**Анализ при закрытии:**
- ✅ Функция **НЕ вызывается** при закрытии (так как useEffect возвращается до её вызова)
- ✅ Внутри функции есть `window.scrollTo` и `scrollableContainer.scrollTo`, но они не выполняются при закрытии

**Вывод:** ✅ `scrollSubcategoryToTop` не влияет на закрытие.

---

## 4️⃣ Проверка функции scrollIntoViewIfNeeded

**Местоположение:** `src/app/uslugi/service-accordion.tsx`, строки 1185-1208

**Когда вызывается:**
- Только из `useEffect` для `openSection` (строка 1620):
```typescript
useEffect(() => {
  if (!openSection) return
  scrollIntoViewIfNeeded(sectionRefs.current[openSection], SECTION_SCROLL_OFFSET)
}, [openSection])
```

**Анализ при закрытии подкатегории:**
- ✅ `scrollIntoViewIfNeeded` срабатывает при изменении `openSection` (открытие/закрытие секции верхнего уровня)
- ✅ При закрытии **подкатегории** `openSection` НЕ изменяется (остается 'naprawy')
- ✅ Функция **НЕ вызывается** при закрытии подкатегории

**Вывод:** ✅ `scrollIntoViewIfNeeded` не влияет на закрытие подкатегории.

---

## 5️⃣ Проверка setTimeout / setInterval / requestAnimationFrame

### 5.1 Все использования setTimeout

**Найдено:** 12 использований `setTimeout` в файле

**Проверка каждого:**

1. **Строки 470-472:** `setTimeout(measureColumns, ...)` — для измерения размеров колонок таблицы
   - ✅ Не связано с закрытием подкатегории
   - ✅ Только измерения размеров

2. **Строка 503:** `setTimeout(measureHeader, 600)` — для измерения заголовка
   - ✅ Не связано с закрытием подкатегории
   - ✅ Только измерения размеров

3. **Строки 905, 921:** `setTimeout(() => { ... }, 150)` — для измерения ширины контейнеров
   - ✅ Не связано с закрытием подкатегории
   - ✅ Только измерения размеров

4. **Строка 1252:** `setTimeout(() => { ... }, 100)` — внутри `scrollSubcategoryToTop`
   - ✅ Не выполняется при закрытии (так как `scrollSubcategoryToTop` не вызывается)

5. **Строка 1302:** `setTimeout(performContainerScroll, 200)` — внутри `scrollSubcategoryToTop`
   - ✅ Не выполняется при закрытии (так как `scrollSubcategoryToTop` не вызывается)

6. **Строки 1698-1700, 1757-1759:** `setTimeout(measureAll, ...)` — для измерения позиций столбцов
   - ✅ Не связано с закрытием подкатегории
   - ✅ Только измерения размеров

**Вывод:** ✅ Все `setTimeout` либо не связаны с закрытием подкатегории, либо находятся внутри функций, которые не вызываются при закрытии.

### 5.2 Все использования setInterval

**Найдено:** 0 использований `setInterval` в файле

**Вывод:** ✅ `setInterval` не используется вообще.

### 5.3 Все использования requestAnimationFrame

**Найдено:** 2 использования `requestAnimationFrame` в файле

1. **Строки 1205-1206:** Внутри `scrollIntoViewIfNeeded`
   - ✅ Не выполняется при закрытии подкатегории (так как функция не вызывается)

2. **Строки 1250-1251:** Внутри `scrollSubcategoryToTop`
   - ✅ Не выполняется при закрытии (так как `scrollSubcategoryToTop` не вызывается)

**Вывод:** ✅ `requestAnimationFrame` не выполняется при закрытии подкатегории.

---

## 6️⃣ Проверка CSS классов scroll-smooth и scroll-behavior

### 6.1 CSS scroll-behavior: smooth

**Местоположение:** `src/app/globals.css`, строка 7

**Код:**
```css
html {
  scroll-behavior: smooth;
  scroll-padding-top: 65px;
}
```

**Анализ:**
- ⚠️ `scroll-behavior: smooth` применяется глобально ко всем элементам
- ⚠️ Это означает, что если бы была программная прокрутка (например, через `scrollTo()`), она была бы плавной
- ✅ **НО:** При закрытии подкатегории НЕТ программной прокрутки (проверено выше)
- ✅ `scroll-behavior: smooth` влияет только на программную прокрутку, не на естественное изменение высоты контента
- ✅ При закрытии аккордеона браузер просто уменьшает высоту элемента через CSS анимацию, без вызова `scrollTo()`

**Вывод:** ✅ `scroll-behavior: smooth` **не вызывает** автоматическую прокрутку при закрытии. Он влияет только на программные вызовы `scrollTo()`, которых нет при закрытии.

### 6.2 Tailwind класс scroll-smooth

**Найдено:** 2 использования `scroll-smooth` в файле

1. **Строка 1051:** `className="overflow-x-auto scroll-smooth"` — для мобильной таблицы
   - ✅ Не связано с подкатегориями секции naprawy
   - ✅ Только для горизонтального скролла таблиц

2. **Строка 2167:** `className="... overflow-y-auto scroll-smooth accordion-scroll ..."` — для AccordionContent секции
   - ⚠️ Применяется к контейнеру с `overflow-y-auto` внутри открытой секции
   - ⚠️ Класс `scroll-smooth` делает плавным **скролл внутри контейнера**, но не влияет на прокрутку страницы
   - ✅ При закрытии подкатегории:
     - Контент подкатегории просто исчезает (CSS анимация)
     - Высота контейнера уменьшается естественным образом
     - **НЕТ программной прокрутки** контейнера
   - ✅ `scroll-smooth` влияет только на прокрутку внутри контейнера при программном вызове `scrollTo()` или пользовательском скролле, но не на естественное изменение высоты

**Вывод:** ✅ Класс `scroll-smooth` **не вызывает** автоматическую прокрутку при закрытии. Он влияет только на программную прокрутку контейнера, которой нет при закрытии.

---

## 7️⃣ Проверка других возможных мест влияния на скролл

### 7.1 Radix UI Accordion

**Проверка:** Radix UI Accordion использует CSS анимации для открытия/закрытия контента:
- `animate-accordion-up` при закрытии
- `animate-accordion-down` при открытии

**Анализ:**
- ✅ CSS анимации изменяют только `height`, `max-height`, `opacity` и `transform`
- ✅ CSS анимации **НЕ вызывают** прокрутку страницы или контейнера
- ✅ Браузер естественным образом обрабатывает изменение высоты элемента

**Вывод:** ✅ Radix UI Accordion не влияет на скролл при закрытии.

### 7.2 Глобальные обработчики событий

**Проверка:** Поиск `addEventListener` связанных со скроллом

**Найдено:**
- Строка 508: `window.addEventListener('resize', handleResize)` — только для измерения размеров
- Строка 1706: `window.addEventListener('resize', handleResize)` — только для измерения размеров
- Строка 1763: `window.addEventListener('resize', handleResizeDZ)` — только для измерения размеров

**Вывод:** ✅ Нет обработчиков событий скролла, которые могли бы влиять на закрытие.

---

## 8️⃣ Итоговый вывод

### ✅ Подтверждение пассивного поведения

**При закрытии подкатегории:**

1. ✅ **НЕ вызываются** функции прокрутки:
   - `scrollIntoView` — не вызывается
   - `scrollSubcategoryToTop` — не вызывается (useEffect возвращается до вызова)
   - `scrollTo` / `scrollBy` — не вызываются
   - `window.scrollTo` — не вызывается
   - `container.scrollTo` — не вызывается

2. ✅ **НЕ используются** таймеры для изменения скролла:
   - `setTimeout` — используется только для измерений, не для скролла
   - `setInterval` — не используется вообще
   - `requestAnimationFrame` — не выполняется (находится только в функциях, которые не вызываются)

3. ✅ **CSS классы не вызывают автоматическую прокрутку:**
   - `scroll-behavior: smooth` — влияет только на программную прокрутку (которой нет)
   - `scroll-smooth` — влияет только на программную прокрутку контейнера (которой нет)

### 📝 Фактическое поведение при закрытии

**Последовательность событий:**

1. Пользователь кликает на открытую подкатегорию
2. Вызывается `handleSubcategoryChange(sectionId, null)`
3. Выполняется `setOpenSubcategory(null)` — изменяется состояние React
4. React запускает ре-рендер
5. Radix UI Accordion обновляет `data-state="closed"` на AccordionItem
6. Применяется CSS анимация `animate-accordion-up` — контент плавно сворачивается
7. **Браузер естественным образом обрабатывает уменьшение высоты:**
   - Элементы ниже закрытой подкатегории автоматически подтягиваются вверх
   - Позиция прокрутки страницы остается на том же месте визуально (браузер автоматически корректирует её при необходимости)
   - Нет программного вмешательства в скролл

### ✅ Итоговое подтверждение

**При закрытии подкатегории аккордеона код полностью пассивен:**
- ✅ Браузер сам уменьшает высоту открытого блока через CSS анимацию
- ✅ Всё остальное естественно подтягивается вверх
- ✅ Нет программного вмешательства в прокрутку
- ✅ Нет вызовов функций скролла
- ✅ Нет таймеров, влияющих на скролл
- ✅ CSS классы не вызывают автоматическую прокрутку

**Поведение полностью естественное и предсказуемое.**

---

## 9️⃣ Дополнительные заметки

### Потенциальное влияние scroll-behavior: smooth

Хотя `scroll-behavior: smooth` в `globals.css` применяется глобально, он **не влияет** на закрытие подкатегории, потому что:

1. Нет программных вызовов `scrollTo()` при закрытии
2. `scroll-behavior: smooth` влияет только на:
   - Программные вызовы `Element.scrollTo()`, `window.scrollTo()`
   - Прокрутку через якоря (`<a href="#id">`)
   - **НЕ влияет** на естественное изменение высоты элементов

### Поведение браузера при изменении высоты контента

При закрытии аккордеона браузер:

1. Уменьшает высоту элемента через CSS анимацию
2. Автоматически корректирует позицию прокрутки, чтобы сохранить визуальное положение контента
3. Это **стандартное поведение браузера**, не требует программного вмешательства

Такое поведение является естественным и предпочтительным — браузер сам управляет позицией прокрутки при изменении высоты контента.

---

**Проверка завершена: ✅ Подтверждено пассивное поведение при закрытии**

