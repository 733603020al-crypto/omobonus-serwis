# Анализ генерации номера заявки (Ticket Number)

## 📋 Резюме

Проведён анализ текущей системы генерации номеров заявок для формы "Formularz zgłoszeniowy" на сайте Omobonus Serwis. Обнаружено, что система не использует базу данных, а номер заявки генерируется на основе текущей даты и timestamp, что может привести к коллизиям.

---

## 1. 🔍 Местоположение кода

### 1.1. Форма на фронтенде
**Файл:** `src/components/sections/contact.tsx`

**Функция отправки:** `onSubmit` (строки 137-208)

**Процесс:**
- Форма собирает данные через React Hook Form
- Данные отправляются через `fetch('/api/send-email', { method: 'POST', body: formData })`
- После успешной отправки показывается модальное окно успеха

**Ключевой код:**
```typescript
const onSubmit = async (data: FormValues) => {
  // ... валидация и подготовка данных
  const response = await fetch('/api/send-email', {
    method: 'POST',
    body: formData,
  })
  // ... обработка ответа
}
```

### 1.2. API endpoint обработки формы
**Файл:** `src/app/api/send-email/route.ts`

**Функция:** `POST` (строки 200-755)

**Процесс:**
1. Принимает данные формы через `request.formData()`
2. Генерирует номер заявки через `generateTicketNumber()`
3. Формирует HTML-шаблоны писем (для сервиса и клиента)
4. Отправляет два письма через nodemailer:
   - Письмо сервису (serwis@omobonus.com.pl)
   - Письмо клиенту (подтверждение)

**Ключевой код:**
```typescript
export async function POST(request: NextRequest) {
  // ... получение данных формы
  const ticketNumber = generateTicketNumber() // строка 282
  // ... формирование и отправка писем
}
```

### 1.3. Функция генерации номера заявки
**Файл:** `src/app/api/send-email/route.ts`

**Функция:** `generateTicketNumber()` (строки 135-147)

**Текущая реализация:**
```typescript
const generateTicketNumber = (): string => {
  const now = new Date()
  const day = String(now.getDate()).padStart(2, '0')
  const month = String(now.getMonth() + 1).padStart(2, '0')
  const year = String(now.getFullYear()).slice(-2)

  // Используем последние 3 цифры timestamp для уникальности
  const timestamp = Date.now()
  const sequence = String(timestamp).slice(-3)

  return `${day}${month}${year}-${sequence}`
}
```

---

## 2. 📊 Текущая логика генерации номера

### 2.1. Первая часть (дата): `DDMMYY`
- **Формат:** День + Месяц + Год (последние 2 цифры)
- **Пример:** `121225` = 12 декабря 2025 года
- **Источник:** Текущая дата через `new Date()`
- **Статус:** ✅ Работает корректно

### 2.2. Вторая часть (последовательность): `XXX`
- **Формат:** 3 цифры
- **Источник:** Последние 3 цифры `Date.now()` (timestamp в миллисекундах)
- **Пример:** `693` из timestamp `1734012345693`
- **Проблема:** ⚠️ **НЕ ГАРАНТИРУЕТ УНИКАЛЬНОСТЬ**

### 2.3. Как работает timestamp
- `Date.now()` возвращает количество миллисекунд с 1 января 1970 года
- Последние 3 цифры могут повторяться:
  - Если две заявки созданы в течение одной секунды (или близко по времени)
  - Если разница между timestamp'ами кратна 1000, последние 3 цифры совпадут
  - **Пример коллизии:**
    - Заявка 1: timestamp `1734012345693` → номер `121225-693`
    - Заявка 2: timestamp `1734012346693` → номер `121225-693` (та же дата, те же последние 3 цифры)

---

## 3. ⚠️ Оценка текущей логики

### 3.1. Уникальность номера заявки
**Статус:** ❌ **НЕ ГАРАНТИРОВАНА**

**Проблемы:**
1. **Коллизии возможны:** Если две заявки созданы в одну секунду (или с разницей, кратной 1000 мс), последние 3 цифры timestamp совпадут
2. **Вероятность коллизии:** 
   - В пределах одной секунды: ~0.1% (1 из 1000)
   - В пределах одной минуты: ~1.67% (60 из 3600)
   - При высокой нагрузке (много заявок в секунду) риск значительно возрастает

### 3.2. Привязка к базе данных
**Статус:** ❌ **ОТСУТСТВУЕТ**

**Текущая ситуация:**
- База данных **НЕ используется** в проекте
- Нет Prisma, нет SQL файлов, нет конфигурации БД
- Данные формы **НЕ сохраняются** нигде, кроме email-писем
- Номер заявки генерируется "на лету" и **НЕ привязан** к какой-либо записи

**Последствия:**
- Невозможно отследить заявки в системе
- Невозможно проверить уникальность номера перед отправкой
- Невозможно связать номер заявки с конкретной записью в БД
- Нет истории заявок

### 3.3. Риск коллизий
**Высокий риск при:**
- Одновременных отправках формы (несколько пользователей одновременно)
- Автоматизированных тестах
- Высокой нагрузке на сайт

**Низкий риск при:**
- Низкой частоте отправки форм (1-2 заявки в минуту)
- Но даже в этом случае коллизии теоретически возможны

---

## 4. 💡 Предложение нового варианта

### 4.1. Рекомендуемый формат номера
**Формат:** `DDMMYY-XXX` (остаётся прежним для совместимости)

**Где:**
- `DDMMYY` — дата создания заявки (12.12.2025 → `121225`)
- `XXX` — **последовательный номер заявки за день** (001, 002, 003...)

### 4.2. Варианты реализации второй части

#### Вариант A: Счётчик заявок за день (рекомендуется)
**Логика:**
- При создании заявки получаем ID из базы данных (автоинкремент)
- Или используем счётчик заявок за текущий день
- Форматируем как 3-значное число с ведущими нулями: `001`, `002`, `003`...

**Преимущества:**
- ✅ Гарантированная уникальность (через БД)
- ✅ Последовательность номеров понятна
- ✅ Легко отслеживать количество заявок за день
- ✅ Номер привязан к конкретной записи в БД

**Примеры:**
- Первая заявка 12.12.2025: `121225-001`
- Вторая заявка 12.12.2025: `121225-002`
- Первая заявка 13.12.2025: `131225-001` (счётчик сбрасывается)

#### Вариант B: ID записи в базе данных
**Логика:**
- Используем автоинкрементный ID из БД
- Берём последние 3 цифры ID (или больше, если нужно)

**Преимущества:**
- ✅ Гарантированная уникальность
- ✅ Прямая связь с записью в БД

**Недостатки:**
- ⚠️ Номера не будут последовательными (если ID = 1234, номер будет `121225-234`)
- ⚠️ Менее понятно для пользователей

### 4.3. Рекомендация
**Выбрать Вариант A (счётчик за день):**
- Более понятный для клиентов
- Последовательные номера
- Легко отслеживать статистику

---

## 5. 📝 План реализации

### 5.1. Шаг 1: Настройка базы данных

**Действия:**
1. Выбрать БД (рекомендуется PostgreSQL или MySQL)
2. Установить Prisma ORM:
   ```bash
   npm install prisma @prisma/client
   npx prisma init
   ```
3. Создать схему БД (`prisma/schema.prisma`):
   ```prisma
   model Application {
     id            Int      @id @default(autoincrement())
     ticketNumber  String   @unique
     name          String
     phone         String
     email         String
     address       String
     deviceType    String
     deviceModel   String?
     problemDescription String
     replacementPrinter Boolean
     createdAt     DateTime @default(now())
     updatedAt     DateTime @updatedAt
   }
   ```

4. Создать миграцию:
   ```bash
   npx prisma migrate dev --name init
   ```

**Файлы для изменения:**
- Создать: `prisma/schema.prisma`
- Обновить: `package.json` (добавить зависимости)

---

### 5.2. Шаг 2: Создание функции генерации номера с БД

**Файл:** `src/app/api/send-email/route.ts`

**Изменения:**
1. Импортировать Prisma Client:
   ```typescript
   import { PrismaClient } from '@prisma/client'
   const prisma = new PrismaClient()
   ```

2. Заменить функцию `generateTicketNumber()`:
   ```typescript
   const generateTicketNumber = async (): Promise<string> => {
     const now = new Date()
     const day = String(now.getDate()).padStart(2, '0')
     const month = String(now.getMonth() + 1).padStart(2, '0')
     const year = String(now.getFullYear()).slice(-2)
     const datePrefix = `${day}${month}${year}`

     // Найти последнюю заявку за сегодня
     const todayStart = new Date(now.setHours(0, 0, 0, 0))
     const todayEnd = new Date(now.setHours(23, 59, 59, 999))
     
     const lastApplication = await prisma.application.findFirst({
       where: {
         createdAt: {
           gte: todayStart,
           lte: todayEnd,
         },
         ticketNumber: {
           startsWith: datePrefix,
         },
       },
       orderBy: {
         createdAt: 'desc',
       },
     })

     // Определить следующий номер
     let sequence = 1
     if (lastApplication) {
       const lastSequence = parseInt(lastApplication.ticketNumber.split('-')[1])
       sequence = lastSequence + 1
     }

     const sequenceStr = String(sequence).padStart(3, '0')
     return `${datePrefix}-${sequenceStr}`
   }
   ```

**Альтернативный вариант (через счётчик в отдельной таблице):**
- Создать таблицу `DailyCounter` для хранения счётчика за день
- Атомарно инкрементировать счётчик через транзакцию

---

### 5.3. Шаг 3: Сохранение заявки в БД

**Файл:** `src/app/api/send-email/route.ts`

**Изменения в функции `POST`:**
1. После генерации номера заявки сохранить данные в БД:
   ```typescript
   const ticketNumber = await generateTicketNumber()
   
   // Сохранить заявку в БД
   const application = await prisma.application.create({
     data: {
       ticketNumber,
       name,
       phone,
       email,
       address,
       deviceType: deviceType.toLowerCase(),
       deviceModel: deviceModel || null,
       problemDescription,
       replacementPrinter: replacementPrinter === 'Tak',
     },
   })
   ```

2. Использовать `ticketNumber` из БД для писем (уже сгенерирован)

**Место в коде:**
- После строки 282 (где сейчас `const ticketNumber = generateTicketNumber()`)
- Перед формированием HTML-шаблонов писем

---

### 5.4. Шаг 4: Обработка ошибок и транзакций

**Добавить:**
1. Обработку ошибок уникальности (на случай редкой коллизии):
   ```typescript
   try {
     const ticketNumber = await generateTicketNumber()
     // ... сохранение в БД
   } catch (error) {
     if (error.code === 'P2002') { // Prisma unique constraint error
       // Повторить генерацию номера
       const ticketNumber = await generateTicketNumber()
       // ... повторная попытка сохранения
     }
   }
   ```

2. Транзакцию для атомарности:
   ```typescript
   await prisma.$transaction(async (tx) => {
     const ticketNumber = await generateTicketNumber(tx)
     const application = await tx.application.create({ ... })
     return application
   })
   ```

---

### 5.5. Шаг 5: Обновление переменных окружения

**Файл:** `.env.local` (или `.env`)

**Добавить:**
```env
DATABASE_URL="postgresql://user:password@localhost:5432/omobonus_serwis?schema=public"
```

**Для продакшена:**
- Настроить БД на хостинге (Vercel, Railway, Supabase и т.д.)
- Добавить `DATABASE_URL` в переменные окружения

---

### 5.6. Шаг 6: Тестирование

**Проверить:**
1. ✅ Генерация уникальных номеров за день
2. ✅ Сброс счётчика на следующий день
3. ✅ Сохранение заявок в БД
4. ✅ Отправка писем с корректным номером
5. ✅ Обработка одновременных запросов (race conditions)

---

## 6. 📂 Сводка файлов для изменения

### 6.1. Новые файлы
- `prisma/schema.prisma` — схема базы данных
- `.env.local` (обновить) — добавить `DATABASE_URL`

### 6.2. Изменяемые файлы
- `src/app/api/send-email/route.ts`:
  - Импорт Prisma Client
  - Замена функции `generateTicketNumber()` на асинхронную версию с БД
  - Добавление сохранения заявки в БД перед отправкой писем
  - Обработка ошибок и транзакций

- `package.json`:
  - Добавить зависимости: `prisma`, `@prisma/client`

### 6.3. Опциональные файлы
- `src/lib/prisma.ts` — синглтон Prisma Client (рекомендуется для production)
- `src/app/api/applications/route.ts` — API для получения списка заявок (для админки)

---

## 7. ⚙️ Альтернативный вариант (без БД)

Если по каким-то причинам нельзя использовать БД, можно улучшить текущую логику:

### 7.1. Улучшенная генерация без БД
```typescript
const generateTicketNumber = (): string => {
  const now = new Date()
  const day = String(now.getDate()).padStart(2, '0')
  const month = String(now.getMonth() + 1).padStart(2, '0')
  const year = String(now.getFullYear()).slice(-2)

  // Используем больше цифр timestamp + случайное число
  const timestamp = Date.now()
  const random = Math.floor(Math.random() * 100)
  const sequence = String(timestamp).slice(-2) + String(random).padStart(1, '0')
  
  return `${day}${month}${year}-${sequence}`
}
```

**Но это НЕ РЕКОМЕНДУЕТСЯ**, так как:
- ❌ Всё ещё возможны коллизии (хотя вероятность ниже)
- ❌ Нет привязки к записи
- ❌ Нет истории заявок

---

## 8. ✅ Итоговые рекомендации

1. **Обязательно внедрить базу данных** для надёжной системы
2. **Использовать счётчик за день** (Вариант A) для понятных номеров
3. **Сохранять заявки в БД** перед отправкой писем
4. **Добавить обработку ошибок** для редких случаев коллизий
5. **Протестировать** одновременные запросы

---

## 9. 📊 Сравнение: До и После

| Параметр | Текущая система | Предлагаемая система |
|----------|----------------|---------------------|
| Уникальность | ❌ Не гарантирована | ✅ Гарантирована (БД) |
| Привязка к записи | ❌ Нет | ✅ Есть (ID в БД) |
| Последовательность | ❌ Случайная | ✅ Последовательная |
| История заявок | ❌ Нет | ✅ Есть (БД) |
| Риск коллизий | ⚠️ Высокий | ✅ Минимальный |
| Понятность номера | ✅ Понятно | ✅ Понятно |

---

**Дата анализа:** 2025-01-XX  
**Версия документа:** 1.0

